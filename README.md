# untrusted-content-guard
Claude Code PostToolUse hook: detects prompt-injection and credential-leak patterns in fetched web content, quarantines findings without leaking the secret, never blocks the workflow. Ships measured precision/recall.
